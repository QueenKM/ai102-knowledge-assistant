# Raw Documents Folder

Put `PDF` files or scanned images here before running:

```bash
python3 scripts/import_with_document_intelligence.py --input-dir data/raw_documents
```

The import script converts supported files into markdown documents under `data/source_documents`.

